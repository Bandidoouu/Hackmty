# === Backend configuration ===
# En tu red HTTPS (443) pudo estar bloqueado; usamos HTTP por defecto.
USE_NESSIE=true
NESSIE_BASE_URL=http://api.nessieisreal.com
NESSIE_API_KEY=REEMPLAZA_CON_TU_KEY
DEFAULT_NESSIE_ACCOUNT_ID=ACC_XXXXXXXX

# Gemini (opcional). Si no pones API key, el coach responde en modo básico.
GEMINI_API_KEY=REEMPLAZA_CON_TU_GEMINI_KEY
GEMINI_MODEL=gemini-2.5-pro

# Puertos (opcional; no estrictamente necesarios)
BACKEND_PORT=8000
FRONTEND_PORT=5173
